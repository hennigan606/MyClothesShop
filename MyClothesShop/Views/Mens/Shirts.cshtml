@model IPagedList<Product>

@{
    ViewBag.Title = "Mens Shirts";
}

<h1>Men's Shirts</h1>

@Html.Partial("MensNavBar")

<div>
    @Html.Partial("Products")

    @*<div class="pager">*@
        @Html.PagedListPager(Model, page => Url.Action("Shirts", new { page }),
        PagedListRenderOptions.MinimalWithItemCountText)
    @*</div>*@
</div>